library;

/// Placeholder enums for Week 1 domain modeling.
/// Expand these once the actual entities are implemented.

enum ConceptCategory { basics, widgets, stateManagement, networking, testing }

enum DifficultyLevel { beginner, intermediate, advanced }

enum ReviewRating { again, hard, good, easy }