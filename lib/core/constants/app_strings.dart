class AppStrings {
  const AppStrings._();

  static const appName = 'Flutter Learning Hub';
  static const appTagline = 'Supabase foundation setup in progress';
  static const initialRouteLabel = 'Initial project structure is ready.';
  static const supabasePendingLabel =
      'Configure SUPABASE_URL and SUPABASE_ANON_KEY to enable backend tracking.';
}
