import 'package:flutter/material.dart';

/// Lumina Learning System color palette — Corporate Modern aesthetic.
class AppColors {
  const AppColors._();

  // ── Primary / Indigo ──────────────────────────────────────────────────────
  static const primary = Color(0xFF3525CD);
  static const onPrimary = Color(0xFFFFFFFF);
  static const primaryContainer = Color(0xFF4F46E5);
  static const onPrimaryContainer = Color(0xFFDAD7FF);
  static const inversePrimary = Color(0xFFC3C0FF);

  static const primaryFixed = Color(0xFFE2DFFF);
  static const primaryFixedDim = Color(0xFFC3C0FF);
  static const onPrimaryFixed = Color(0xFF0F0069);
  static const onPrimaryFixedVariant = Color(0xFF3323CC);

  // ── Secondary / Cyan ──────────────────────────────────────────────────────
  static const secondary = Color(0xFF00687A);
  static const onSecondary = Color(0xFFFFFFFF);
  static const secondaryContainer = Color(0xFF57DFFE);
  static const onSecondaryContainer = Color(0xFF006172);

  static const secondaryFixed = Color(0xFFACEDFF);
  static const secondaryFixedDim = Color(0xFF4CD7F6);
  static const onSecondaryFixed = Color(0xFF001F26);
  static const onSecondaryFixedVariant = Color(0xFF004E5C);

  // ── Tertiary / Mastery Gold (Amber) ───────────────────────────────────────
  static const tertiary = Color(0xFF684000);
  static const onTertiary = Color(0xFFFFFFFF);
  static const tertiaryContainer = Color(0xFF885500);
  static const onTertiaryContainer = Color(0xFFFFD4A4);

  static const tertiaryFixed = Color(0xFFFFDDB8);
  static const tertiaryFixedDim = Color(0xFFFFB95F);
  static const onTertiaryFixed = Color(0xFF2A1700);
  static const onTertiaryFixedVariant = Color(0xFF653E00);

  // ── Error ─────────────────────────────────────────────────────────────────
  static const error = Color(0xFFBA1A1A);
  static const onError = Color(0xFFFFFFFF);
  static const errorContainer = Color(0xFFFFDAD6);
  static const onErrorContainer = Color(0xFF93000A);

  // ── Success / Emerald ─────────────────────────────────────────────────────
  static const emerald = Color(0xFF10B981);
  static const onEmerald = Color(0xFFFFFFFF);

  // ── Surface ───────────────────────────────────────────────────────────────
  static const surface = Color(0xFFF9F9FF);
  static const surfaceDim = Color(0xFFCFDAF2);
  static const surfaceBright = Color(0xFFF9F9FF);
  static const surfaceContainerLowest = Color(0xFFFFFFFF);
  static const surfaceContainerLow = Color(0xFFF0F3FF);
  static const surfaceContainer = Color(0xFFE7EEFF);
  static const surfaceContainerHigh = Color(0xFFDEE8FF);
  static const surfaceContainerHighest = Color(0xFFD8E3FB);
  static const surfaceVariant = Color(0xFFD8E3FB);
  static const surfaceTint = Color(0xFF4D44E3);
  static const inverseSurface = Color(0xFF263143);
  static const inverseOnSurface = Color(0xFFECF1FF);

  // ── On-Surface ────────────────────────────────────────────────────────────
  static const onSurface = Color(0xFF111C2D);
  static const onSurfaceVariant = Color(0xFF464555);

  // ── Outline ───────────────────────────────────────────────────────────────
  static const outline = Color(0xFF777587);
  static const outlineVariant = Color(0xFFC7C4D8);

  // ── Background ────────────────────────────────────────────────────────────
  static const background = Color(0xFFF0F3FF); // surfaceContainerLow
  static const onBackground = Color(0xFF111C2D);
}
